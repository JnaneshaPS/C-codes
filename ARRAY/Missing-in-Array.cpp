class solution{
//given the size of the array we need to find the missing number in the array

int missingArray(vector<int> &arr,int n)
{
    int sun=(n*n(+1)/2);

int expectedSum=0;

   for(int i=0;i<arr.size();i++)
{
       exprectedSum=expectedSum+arr[i];
}

int ans=sum-expectedSu;
return ans;
}
};
